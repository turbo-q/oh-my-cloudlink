---
description: 先读 docs/AGENTS.md；功能/架构变更后须同步更新以持续对齐
alwaysApply: true
---

# 先读并持续对齐 AGENTS.md

## 动手前

1. **先阅读** [`docs/AGENTS.md`](docs/AGENTS.md)（根目录 `AGENTS.md` 仅为入口指针）。
2. 至少理解：主进程 vs 渲染进程边界、userData 路径、SSH 终端热路径、i18n / IPC 约定、提交与「不要做」项。
3. 若任务涉及终端性能或实验特性，再按该文档「推荐阅读顺序」打开对应 `docs/` 与源码。

未阅读 `docs/AGENTS.md` 时，不要假设目录职责、数据路径或终端实现细节。

## 改完后：与导读持续对齐

本轮若包含**功能性修改**，或改变了 Agent 后续必须知道的约定，**必须在同一轮**更新 [`docs/AGENTS.md`](docs/AGENTS.md)（必要时同步根目录指针说明），避免导读过时。

需要更新的典型情况：

- 新模块 / 目录、主↔渲染边界或 IPC / `electronAPI` 约定变化
- 默认终端路径、热路径、搜索 / WebGL / MessagePort 等行为变化
- userData、日志保留、加密 / 备份策略变化
- 开发命令、打包纳入物（如原生 `.node`）、分支职责变化
- 「不要做」或检查清单需要增删

纯文案 typo、与架构无关的局部 UI 微调、未影响约定的小 bugfix：**不必**为改而改 AGENTS.md。

更新时只改正文中过时段落，保持简洁，勿把实现细节全文搬进导读。
