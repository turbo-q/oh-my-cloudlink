---
description: 代码改完后的 git 提交策略（完成项可自动提交；重大/未完成/不明则先问）
alwaysApply: true
---

# Git 提交规范

## 默认：可自动提交

在**本轮任务目标已完成**、改动意图清晰、工作区不含半成品时，修改完成后**立即** `git commit`，无需再等用户确认。

流程：

1. 根据**全量**相关 diff 写 commit 文案（用户若要求全量文案，按当前仓库全部未提交变更写一条）
2. `git add` 仅暂存相关文件（排除 `.env`、密钥、凭证等）
3. HEREDOC 提交：

```bash
git commit -m "$(cat <<'EOF'
<commit message>
EOF
)"
```

4. `git status` 确认成功

## 必须先询问用户是否提交

出现以下任一情况时，**不要自动 commit**；先简要说明现状，并明确问「是否现在提交？」：

1. **重大更新**：架构调整、数据格式 / 协议变更、默认行为或设置项变更、打包 / 发布 / 版本号、跨多子系统的大范围改动
2. **未完成**：功能或优化只做了一部分、有已知失败测试 / 编译错误、TODO 仍挡主路径、用户说「先这样 / 还没做完」
3. **意图不明确**：用户没说清要不要落库、改动像是探索 / spike、同时存在多组互不相关的脏改动、不确定该不该进本次 commit

询问时应包含：改了什么（1–3 句）、为何犹豫、建议的 commit 文案（可选）。用户明确同意后再按上面流程提交。

## Commit 类型

- `feat`: 新功能
- `fix`: Bug 修复
- `refactor`: 重构（不改变外部行为）
- `test`: 测试
- `docs`: 文档
- `chore`: CI/CD、构建配置

## 注意

- 不要 push，除非用户明确要求
- 不要提交含 secrets 的文件
- commit 消息聚焦「为什么」，1–2 句为宜
- 用户当次指示（例如「先别提交」「现在提交」）**优先于**本规则
- 与用户其他提交类规则冲突时，以**更谨慎、先询问**的一方为准
